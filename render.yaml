services:
  - type: web
    name: digital-success
    runtime: node
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: FRONTEND_URL
        fromService:
          type: web
          property: host
          prefix: https://
